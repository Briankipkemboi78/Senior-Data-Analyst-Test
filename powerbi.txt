'''you need a measure that shows the repayment collection rate: 
total amount collected divided by total amount scheduled, within the current filter context.'''


Collection Rate =
DIVIDE(
  SUM(Repayments[amount_paid]),
  SUM(Schedules[expected_amount])
)

---------------------------------------------------------------
'''loan disbursements compared to the same month last year (SPLY) in a single visual. Write the correct DAX measure.'''


SPLY Disbursements =
CALCULATE(
  SUM(Loans[amount]),
  SAMEPERIODLASTYEAR(DateTable[Date])
)


----------------------------------------------------------------------------
'''You need a card visual that always shows the national PAR30 regardless of which branch is selected. What DAX pattern achieves this?'''

National PAR30 =
CALCULATE(
  DIVIDE(
    SUMX(ALL(Loans), Loans[is_par30] * Loans[outstanding_balance]),
    SUMX(ALL(Loans), Loans[outstanding_balance])
  )
)


------------------------------------------------------------------------------------------------
'''You need a measure showing loans active AS OF the selected date, 
not just loans disbursed on that date. Which DAX is correct?'''

Active Loans As Of Date =
CALCULATE(
  COUNTROWS(Loans),
  Loans[disbursement_date] <= MAX(DateTable[Date]),
  Loans[maturity_date] >= MAX(DateTable[Date])
)

--------------------------------------------------------------------------------------------------
The Company pilots a new credit scoring model in one region. Default rate drops from 18% to 14%. 
p-value = 0.03, alpha = 0.05, n = 800 clients. What is the correct conclusion?

A. Reject the null hypothesis — the 4% reduction is statistically significant at alpha=0.05. Before full rollout, 
assess practical significance (cost savings vs implementation cost) and whether the pilot region is representative.

-----------------------------------------------------------------------------------------------

The company's default prediction model has precision = 0.72 and recall = 0.41. 
A colleague suggests raising the classification threshold to improve precision. What is the trade-off?

A. Raising the threshold increases precision (fewer false positives) but further decreases recall (more actual defaults are missed). 
Evaluate using F1-score or a business cost matrix weighting missed defaults vs false alerts.


------------------------------------------------------------------------------------------------
You want to test whether repayment rates differ significantly across Company's 5 regional branches. 
Which statistical approach is correct?

A. One-way ANOVA to test whether any branch mean differs. If significant (p < alpha), 
apply Tukey HSD post-hoc test to identify which specific branch pairs differ, controlling family-wise error rate.


------------------------------------------------------------------------------------------------
In a company logistic regression model predicting default, the coefficient for 'loan_term_months' is -0.12. 
What is the correct interpretation?

A. Each additional month of loan term is associated with an approximately 11.3% decrease in the odds of default (odds ratio = e^-0.12 ≈ 0.887), 
holding other variables constant.

Explanation: A negative coefficient means the odds ratio e^-0.12 ≈ 0.887 — each additional month multiplies the odds by 0.887, 
reducing them by ~11.3%. This is NOT the same as a 12% decrease in probability. 
Also, this is an association, not causation — longer-term products may simply attract lower-risk clients.

--------------------------------------------------------------------------------------------------
Company's operations team reports the daily disbursement Power BI dashboard shows zero disbursements for yesterday, 
but the database clearly has records. What are your investigation steps?

1. Check if yesterday's date exists in the DateTable (import may not have refreshed)
2. Verify the relationship between DateTable and Loans is active and correct
3. Check DirectQuery connection — confirm warehouse data is accessible
4. Test the DAX measure in isolation with CALCULATE(..., ALL(DateTable))
5. Check row-level security — ops team may lack access to recent data

--------------------------------------------------------------------------------------------------
Company's CFO asks: 'Our average loan size has grown 22% this year, but total disbursements are flat. 
How is that possible?' How do you explain and investigate this analytically?

A. If average loan size increases but total disbursements stay flat, the number of loans disbursed must have decreased 
proportionally. Investigate: (1) loan count by month and branch, (2) whether new client acquisition has slowed, 
(3) product mix shift toward larger loans for fewer clients, (4) operational capacity constraints.

Explanation: Total = Average x Count. If average rises 22% and total is flat, loan count must have fallen by ~18%. 
The analytical story: Watu may be serving fewer but wealthier clients, or facing acquisition slowdown, 
or making a strategic shift. Breaking down by branch and product type confirms which.
